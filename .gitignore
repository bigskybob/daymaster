node_modules/
docs/
*.log
.DS_Store
.vite/

# Personal Daymaster store dumps / backups — never commit user data
daymaster-data.json
daymaster-*.json

# Wrangler local secrets (NOTION_TOKEN etc.) — never commit
.dev.vars
.dev.vars.*
